<footer class="footer">
    <div class="container utl-space-between">
        <p>&copy; {{ author.name }}</p>
        <p>
            <span>👀 :</span>
            <span id="views">Couldn't load views</span>
        </p>
        <p>{{ strings.labels.lastUpdated }}: {{ build.timestamp | dateToFormat('dd.MM.yyyy') }}</p>
    </div>
</footer>